/**
 * Frontend interactivity for registration prompt block
 *
 * @package WC_PCR
 * @since 2.0.0
 */

(function () {
	'use strict';

	document.addEventListener('DOMContentLoaded', function () {
		const blocks = document.querySelectorAll(
			'.wc-pcr-registration-prompt-block'
		);

		blocks.forEach((block) => {
			// Add any frontend-specific JavaScript here
			// Example: analytics tracking, enhanced UX, etc.
		});
	});
})();
